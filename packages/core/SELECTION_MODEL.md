Selection Model:

- selectedNodeIds: list of node IDs
- selectedEdgeIds: list of edge IDs

Supported actions:
- selectNode(id)
- selectEdge(id)
- selectNodes(ids)
- selectEdges(ids)
- clearSelection()